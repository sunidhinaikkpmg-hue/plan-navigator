import { PagePlaceholder } from "./PagePlaceholder";

export function WhatIfSimulator() {
  return (
    <PagePlaceholder
      title="What-If Simulator"
      description="Ready for AI scenario and what-if simulation content."
    />
  );
}
